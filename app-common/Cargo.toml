[package]
name = "app-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["std", "aya"]
std = []

[dependencies]
aya = { git = "https://github.com/aya-rs/aya", optional = true }
aya-bpf = { git = "https://github.com/aya-rs/aya" }
network-types = "0.0.5"
